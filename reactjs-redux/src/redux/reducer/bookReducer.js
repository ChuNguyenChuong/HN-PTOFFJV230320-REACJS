
const INIT_STATE_BOOK = {
    booksList : [],
    booksDetail : null
}

const bookReducer = (state = INIT_STATE_BOOK, action) => {
    // console.log(state, action);

    return state
}

export default bookReducer